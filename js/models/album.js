class Album {
    constructor(pid, ptitle, puserId) {
        this.id = pid;
        this.title = ptitle;
        this.userId = puserId;
        // this.album = palbum;
        this.photos = [];
    }
}